Расписание храниться в виде json файла, в котором записан json обьект, содержащий массив объектов days (массив дней). 
В массиве days распологаются объекты, которые содержат массив объектов pairs (массив пар).
В массиве pairs находятся объекты (пары), содержащие информацию о таких параметрах как:
  * name - название пары
  * aud - название аудитории, в которой проходит пара
  * pairType - тип пары(1 - практика, 0 - лекция, -1 - ничего)
  * start - время начала пары
  * end - время окончания пары
